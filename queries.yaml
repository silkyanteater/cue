targeting 4.30 FE:
  name: 430FE
  passive: true
  jql: project not in (QAT) AND "Target Version/s" in (4_30_FE)
targeting 4.31:
  name: "431"
  passive: true
  jql: project in ("Trading UI Team") AND "Target Version/s" in (4_31)
targeting 4.30 FE:
  name: 431FE
  passive: true
  jql: project not in (QAT) AND "Target Version/s" in (4_31_FE)

active UI epics:
  name: epics
  passive: true
  jql: project = "Trading UI Team" and issuetype = Epic and status != Closed

merge to master:
  name: mtm
  jql: project = UI and status = "Merge To Master DOING" and "Epic Link" not in (UI-771, UI-1010)
issues of Gabs:
  name: gabs
  jql: (assignee = gabriella.libonati or labels in (GL)) and status != closed and (createdDate > 2019-01-01 or status != backlog)
issues of Henry:
  name: henry
  jql: (assignee = henry.miller or labels in (HM)) and status != closed and (createdDate > 2019-01-01 or status != backlog)
issues of Marco:
  name: marco
  jql: (assignee = marco.diener or labels in (MD)) and status != closed and (createdDate > 2019-01-01 or status != backlog)
issues of Michael:
  name: michael
  jql: (assignee = michael.whelan or labels in (MW)) and status != closed and (createdDate > 2019-01-01 or status != backlog)
issues of Nuno:
  name: nuno
  jql: (assignee = nuno.bettencourt or labels in (NB)) and status != closed and (createdDate > 2019-01-01 or status != backlog)
issues of Peter:
  name: peter
  passive: true
  jql: (assignee = peter.olah or labels in (PO)) and status != closed and (createdDate > 2019-01-01 or status != backlog)
issues of Raj:
  name: raj
  jql: (assignee = raj.thota or labels in (RT)) and status != closed and (createdDate > 2019-01-01 or status != backlog)
